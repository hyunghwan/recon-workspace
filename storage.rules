rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/workspaces/{workspaceId}/periods/{periodId}/imports/{importId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
